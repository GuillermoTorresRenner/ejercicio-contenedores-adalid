services:
  web:
    build:
      context: . # Cambiar el contexto a la raíz del proyecto
      dockerfile: nginx/Dockerfile # Especificar dónde está el Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - db

  db:
    image: postgres:15
    environment:
      POSTGRES_DB: inmobiliaria_db
      POSTGRES_USER: admin # Corregido el typo
      POSTGRES_PASSWORD: password123
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
